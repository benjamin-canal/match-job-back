{% extends 'base.html.twig' %}

{% block title %}Modifier type contract{% endblock %}

{% block body %}
<div class="container">
    <h1>Modifier le type de contract</h1>

    {{ include('contract/_form.html.twig', {'button_label': 'Valider'}) }}

    <a href="{{ path('back_contract_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a>

    {{ include('contract/_delete_form.html.twig') }}
</div>
{% endblock %}
