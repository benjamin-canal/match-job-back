{% extends 'base.html.twig' %}

{% block title %}Contrat{% endblock %}

{% block body %}
<div class="container">
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contract.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contract.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ contract.createdAt ? contract.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ contract.updatedAt ? contract.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_contract_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a>

    <a href="{{ path('back_contract_edit', {'id': contract.id}) }}" class="btn btn-success btn-sm float-end">Modifier</a>

    {{ include('contract/_delete_form.html.twig') }}
</div>
{% endblock %}
