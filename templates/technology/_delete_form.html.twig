<form method="post" action="{{ path('back_technology_delete', {'id': technology.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ technology.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
