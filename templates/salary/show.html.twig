{% extends 'base.html.twig' %}

{% block title %}Salary{% endblock %}

{% block body %}
    <h1>Salary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salary.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ salary.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ salary.createdAt ? salary.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ salary.updatedAt ? salary.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_salary_index') }}">back to list</a>

    <a href="{{ path('back_salary_edit', {'id': salary.id}) }}">edit</a>

    {{ include('salary/_delete_form.html.twig') }}
{% endblock %}
