{% extends 'base.html.twig' %}

{% block title %}Modifier tranche salaire{% endblock %}

{% block body %}
<div class="container">
    <h1>Modifier tranche de salaire</h1>

    {{ include('salary/_form.html.twig', {'button_label': 'Valider'}) }}

    <a href="{{ path('back_salary_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a>

    {{ include('salary/_delete_form.html.twig') }}
</div>
{% endblock %}
