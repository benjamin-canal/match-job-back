{% extends 'base.html.twig' %}

{% block title %}Ajout expérience{% endblock %}

{% block body %}
<div class="container">
    <h1>Ajout d'un niveau d'expérience</h1>

    {{ include('experience/_form.html.twig') }}

    <a href="{{ path('back_experience_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a>
</div>
{% endblock %}
