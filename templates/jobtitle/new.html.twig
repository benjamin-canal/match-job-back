{% extends 'base.html.twig' %}

{% block title %}Ajout titre emploi{% endblock %}

{% block body %}
<div class="container">
    <h1>Ajout d'un titre d'emploi</h1>

    {{ include('jobtitle/_form.html.twig') }}

    <a href="{{ path('back_jobtitle_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a>
</div>
{% endblock %}
