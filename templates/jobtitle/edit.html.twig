{% extends 'base.html.twig' %}

{% block title %}Modifier titre emploi{% endblock %}

{% block body %}
<div class="container">
    <h1>Modifier le titre de l'emploi</h1>

    {{ include('jobtitle/_form.html.twig', {'button_label': 'Valider'}) }}

    <a href="{{ path('back_jobtitle_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a>

    {{ include('jobtitle/_delete_form.html.twig') }}
</div>
{% endblock %}
