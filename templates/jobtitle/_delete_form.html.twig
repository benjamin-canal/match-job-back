<form method="post" action="{{ path('back_jobtitle_delete', {'id': jobtitle.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jobtitle.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
