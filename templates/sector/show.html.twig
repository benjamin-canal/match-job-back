{% extends 'base.html.twig' %}

{% block title %}Sector{% endblock %}

{% block body %}
<div class="container">
    <h1>Sector</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sector.id }}</td>
            </tr>
            <tr>
                <th>SectorName</th>
                <td>{{ sector.sectorName }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ sector.createdAt ? sector.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ sector.updatedAt ? sector.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_sector_index') }}">back to list</a>

    <a href="{{ path('back_sector_edit', {'id': sector.id}) }}">edit</a>

    {{ include('sector/_delete_form.html.twig') }}
</div>
{% endblock %}
